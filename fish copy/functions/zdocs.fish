# Quick jump to Documents directory
# Usage: zdocs
# Jumps to ~/Documents directory

function zdocs -d "Quick jump to Documents directory"
    cd "$HOME/Documents"
    and echo "Jumped to Documents directory"
end
