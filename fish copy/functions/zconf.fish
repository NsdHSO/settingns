# Quick jump to config directory
# Usage: zconf
# Jumps to ~/.config directory

function zconf -d "Quick jump to config directory"
    cd "$HOME/.config"
    and echo "Jumped to config directory"
end
