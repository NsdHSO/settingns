# Fisher Plugin Manager Configuration
# Auto-update Fisher plugins on shell startup (optional)

# Uncomment the following to enable auto-update on startup
# Note: This may slow down shell startup time
# fisher update > /dev/null 2>&1 &

# Fisher is installed and ready to use
# Install plugins with: fisher install <plugin-name>
# List plugins with: fisher list
# Update plugins with: fisher update
