# Navigation shortcuts - use dots to go up directory levels
# Usage: .. (up 1), ... (up 2), .... (up 3), ..... (up 4), ...... (up 5)

function ...... { cd ../../../.. }
function ..... { cd ../../.. }
function .... { cd ../.. }
function ... { cd ../.. }
function .. { cd .. }
function . { cd . }